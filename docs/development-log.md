# 开发日志

## 2026-09-07

- 完成首个原生 Go/Win32 托盘控制器 MVP，使用外置 `sing-box.exe`；
- 将托盘消息循环固定在创建窗口的系统线程，并收紧进程、HTTP 客户端和退出
  清理的生命周期；
- 支持自定义 Homepage 地址和逐显示器 DPI 感知；
- 通过运行中的 Clash API 显示 provider 展开的选择器节点，并加入节点记忆、
  内置国家/地区旗帜资源和原生菜单位图渲染；
- 内核失败后立即清理系统代理；
- Restart core 会先校验并重新读取源 JSON/BPF 配置，再替换运行中的内核；
- 复现新版 Go 工具链的 32 MiB FIPS 熵源缓冲区回归后，将 Go 1.25.6 选为
  Windows 低内存构建基线；
- 将版本化示例和文档与本地运行输出分开。

## 维护规则

每个提交只包含一个明确的行为变化及其回归检查。只记录能帮助后续维护者复现
或理解结果的决定；临时测试 EXE 不进入 Git。
